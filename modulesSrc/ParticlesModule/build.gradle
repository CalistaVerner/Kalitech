plugins {
    id 'java-library'
}

version = '1.0.0'

dependencies {
    implementation project(':kalitech-engine')
    implementation project(':kalitech-core')
}

tasks.named('jar') {
    archiveBaseName.set('ParticlesModule')
}
