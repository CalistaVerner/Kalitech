# MaterialsRegistry

> **Встроенный реестр материалов Kalitech**
> Автор: **Calista Verner**

`MaterialsRegistry` — это декларативный и безопасный слой доступа к материалам движка Kalitech. Он предназначен для того, чтобы:

* хранить **описания материалов в одном месте** (JSON),
* предоставлять **удобный API** для JS‑кода,
* скрывать детали Java / jME Material API,
* обеспечивать **кеширование**, **переиспользование** и **переопределения** материалов,
* быть дружественным к IDE (TypeScript declarations).

---

## Общая идея

Материалы **не создаются напрямую** в игровом коде.

Вместо этого:

1. Все базовые материалы описываются в `materials.json`.
2. JS‑код **запрашивает материал по имени**.
3. Реестр сам:

    * загружает JSON,
    * создаёт Material / Handle через Java API,
    * кеширует результат,
    * применяет overrides.

```text
materials.json → MaterialsRegistry → engine.material().create()
```

---

## Подключение

### Как builtin‑модуль

```js
const M = require("@builtin/MaterialsRegistry")(K);
```

### Через глобалы (bootstrap)

Если `exposeGlobals=true`:

```js
M.get("box");
materials.get("box"); // alias
```

---

## Базовый JSON (materials.json)

```json
{
  "box": {
    "def": "Common/MatDefs/Misc/Unshaded.j3md",
    "params": {
      "Color": [1, 1, 1, 1]
    }
  },
  "terrain": {
    "def": "Common/MatDefs/Light/PBR.j3md",
    "params": {
      "Metallic": 0.0,
      "Roughness": 1.0
    },
    "scales": {
      "TexCoord": 4
    }
  }
}
```

---

## Основные методы API

### `M.get(name, overrides?)`

Возвращает **host Material** (jME Material).

```js
const mat = M.get("box");
geom.setMaterial(mat);
```

* Материал кешируется
* Повторные вызовы дешёвые

---

### `M.getMaterial(name, overrides?)`

Полный аналог `get()`.

```js
const mat = M.getMaterial("box");
```

---

### `M.handle(name, overrides?)`

Возвращает **MaterialHandle** (то, что вернул `engine.material().create`).

Используется, если нужен доступ к id или к handle напрямую.

```js
const h = M.handle("box");
```

---

### `M.getHandle(name, overrides?)`

Полный аналог `handle()`.

---

## Overrides (переопределения)

### Короткая форма (params)

```js
M.get("box", {
  Color: [1, 0, 0, 1]
});
```

Интерпретируется как:

```js
{ params: { Color: [...] } }
```

---

### Полная форма

```js
M.get("terrain", {
  params: {
    Roughness: 0.5
  },
  scales: {
    TexCoord: 8
  }
});
```

---

## Preset — декларативный стиль ⭐

### `M.preset(name, overrides?)`

Создаёт **переиспользуемую фабрику материала**.

```js
const RedBox = M.preset("box", { Color: [1,0,0,1] });

geom.setMaterial(RedBox());
const handle = RedBox.handle();
```

### С дополнительными overrides

```js
RedBox({ Emissive: [0.5,0,0,1] });
```

Presets:

* не создают материал до вызова,
* удобно передаются между системами,
* идеально подходят для UI / FX / оружия.

---

## Удобные хелперы

### `M.params(name, params)`

Быстрый способ задать только параметры:

```js
M.params("box", { Color: [0,1,0,1] });
```

---

### `M.keys()`

Возвращает список доступных материалов:

```js
const all = M.keys();
```

Полезно для:

* дебага,
* редакторов,
* автодополнения.

---

### `M.reload()`

Сбрасывает кеш и перечитывает `materials.json` при следующем доступе.

```js
M.reload();
```

Используется в:

* hot‑reload,
* editor‑режиме,
* дев‑цикле.

---

## Кеширование

### Без overrides

```js
M.get("box"); // создаётся 1 раз
```

Всегда возвращается один и тот же Material.

---

### С overrides

По умолчанию:

* материалы с overrides **кешируются** (FIFO, ограничение по размеру),
* поведение настраивается.

```js
M.configure({
  overrideCache: true,
  overrideCacheMax: 128
});
```

---

## Архитектурные принципы

* ❌ Нет прямого `new Material(...)` в игровом коде
* ✅ Данные отделены от логики
* ✅ Минимум связности с Java
* ✅ Декларативный стиль
* ✅ Поддержка hot‑reload
* ✅ IDE‑friendly (TypeScript)

---

## Когда использовать

Используй `MaterialsRegistry`, если:

* ты пишешь JS‑код поверх Kalitech,
* хочешь централизованно управлять визуальным стилем,
* работаешь с модами,
* делаешь editor / runtime hot‑reload,
* хочешь **чистый и читаемый код**.

---

## Итог

`MaterialsRegistry` — это **не просто хелпер**, а:

> 🔹 *контракт между дизайном, скриптами и движком*

Он делает материалы:

* управляемыми,
* переиспользуемыми,
* декларативными,
* масштабируемыми.

Именно так и должен выглядеть современный скриптовый слой движка Kalitech.
